name: vision-translation
version: 0.2.0
description: "Translation with Visual Primitives — image -> visual primitives (bbox) + spatial relations + OCR as <vision-context> text for text-only models. Core (vision_translation.py) + cross-language CLI protocol (cli.py, PROTOCOL v1) + thin adapters. Hermes native plugin (in-process), works with any auxiliary VLM."
author: "Binglun Li (BingL-Li)"
enabled: true
tools:
  - vision_translate
